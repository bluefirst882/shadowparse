# 开发与验证

## 提交检查

安装根目录依赖后，Husky 会在提交前执行 `lint-staged`。JavaScript、Vue、JSON、CSS、YAML 和 Markdown 文件使用 Prettier；Python worker 运行语法编译检查。

```powershell
D:\program\nodejs\npm.cmd install --cache .npm-cache
D:\program\nodejs\npm.cmd run format:check
D:\program\anaconda\python.exe -m py_compile workers/whisper_worker.py
```

## 完整验证

```powershell
$env:JAVA_HOME='D:\program\idea\jbr'
.\mvnw.cmd -f backend\pom.xml verify
D:\program\nodejs\npm.cmd --prefix frontend run build
```

本机没有 MySQL 时，`MySqlMigrationTest` 会跳过。CI 启动 MySQL 8.4，执行 Flyway 迁移及完整后端验证。

## 2026-09-20 实际复验

在 Docker MySQL 8.4 空数据库（本机 `3307` 端口）上执行：

```powershell
$env:MYSQL_TEST_URL='jdbc:mysql://localhost:3307/video_workbench'
$env:MYSQL_TEST_USERNAME='video_workbench'
$env:MYSQL_TEST_PASSWORD='<本地测试密码>'
.\mvnw.cmd -f backend\pom.xml -Dtest=MySqlMigrationTest test
.\mvnw.cmd -f backend\pom.xml verify
npm --prefix frontend run build
```

结果：Flyway 从空 schema 成功执行 V1、V2；后端 11 项测试通过；前端类型检查与生产构建通过。Flyway 同时提示当前版本尚未声明测试 MySQL 8.4，因此该结果仅作为本次环境的实际验证记录。

## 2026-09-21 中文样本实测

通过本地 API 上传 `E:\Downloads\Video\27210678708-1-192.mp4`，任务使用 Whisper `turbo` 和 RTX 4060 Laptop GPU 完成中文转写。媒体时长 355.947 秒，任务从上传到 `COMPLETED/SUMMARY` 约 58 秒，持久化 250 个片段；摘要未自动执行，符合“转写完成后可独立触发”的当前行为。样本尾部出现超出媒体时长的片段，已记录为待修复的时间边界问题，未作为已通过证据。
