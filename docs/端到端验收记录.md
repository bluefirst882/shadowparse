# 端到端验收记录

验收日期：2026-09-09。

## 已验证

- 后端 Maven 编译与 JUnit：章节来源、时间范围校验通过；MySQL 集成测试在未配置测试库时安全跳过。
- 前端 `vue-tsc` 与 Vite 生产构建通过。
- Playwright 使用系统 Chrome 验证任务工作台桌面、移动和暗色模式渲染，确认导入按钮、空任务提示和主题切换可用。
- Spring Boot + SQLite 临时实例曾验证任务列表 HTTP 200；正式配置已切换为 MySQL + Flyway。
- coderplan.ai：模型列表确认 `gpt-5.6-terra`；合成转写验证 `chat/completions`、`reasoning_effort=low` 和结构化摘要/章节 JSON。

## 待有环境后验证

- MySQL 8.4 实例：执行 `docker compose up -d mysql` 后设置 `MYSQL_*`，运行 `MySqlMigrationTest`。
- FFmpeg 与真实短视频：验证损坏视频、无音轨、中英文语音、取消、超时和重启恢复；当前机器未发现 FFmpeg 可执行文件。
- Whisper `turbo` 模型：Python 3.13 已能导入 `openai-whisper` 与 PyTorch，模型文件须在首次运行时下载到 `models/whisper`。
- 取得有效 MySQL 与本地媒体后，再进行完整导入到导出的真实闭环验收。

所有验收脚本使用隔离输出目录；临时数据库和服务进程在验证后清理。API 密钥不写入源码、日志或导出文件。
