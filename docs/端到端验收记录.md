# 端到端验收记录

验收日期：2026-09-10。

## 已验证

- 后端 Maven 编译与 JUnit：章节来源、时间范围校验通过；MySQL 集成测试通过。
- 前端 `vue-tsc` 与 Vite 生产构建通过。
- Playwright 使用系统 Chrome 验证任务工作台桌面、移动和暗色模式渲染，确认导入按钮、空任务提示和主题切换可用。
- 后端以 MySQL + Flyway 为正式数据存储配置；未配置测试库时，MySQL 集成测试会安全跳过。
- coderplan.ai：模型列表确认 `gpt-5.6-terra`；合成转写验证 `chat/completions`、`reasoning_effort=low` 和结构化摘要/章节 JSON。
- 真实视频 `E:\Downloads\Video\41611231579-1-192.mp4`：FFmpeg 已验证 H.264 视频和 AAC 音轨可完整解码；抽取的 30 秒样本由本地 Whisper `turbo` 在 GPU 上完成英文识别，产生 5 个带时间戳片段。原视频未改动，临时音频已清理。
- MySQL 8.4 隔离容器：Flyway 在纯净数据库成功执行 V1、V2 两项当前迁移；真实视频已通过本地 API 导入、音频提取和完整 Whisper `turbo` 转写，持久化 42 个时间戳片段。未配置云端密钥时任务保留转写并提示可单独重试内容生成。视频 Range 请求返回 `206`，Markdown、JSON、SRT 导出均成功。

## 待有环境后验证

- 真实异常视频：仍需验证损坏视频、无音轨、长文本、取消与重启恢复。英文真实转写已完成，中文真实转写待提供合适样本。

所有验收脚本使用隔离输出目录；临时数据库和服务进程在验证后清理。API 密钥不写入源码、日志或导出文件。
